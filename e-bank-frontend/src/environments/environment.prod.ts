export const environment = {
  production: true,
  // During the build process, replace this URL with the actual production API URL
  // Use Angular CLI environment file replacement:
  // ng build --configuration=production --fileReplacements=src/environments/environment.ts,src/environments/environment.prod.ts
  backendHost: 'https://api.example.com'
};
